meta {
    name: Update Shareholder
  type: http
  seq: 1
}

put {
  url: {{baseUrl}}/shareholders/{{shareholderId}}
  body: json
  auth: inherit
}
headers {
  Authorization: {{PLACEHOLDER}}
}

body:json {
  {
    "email": "{{PLACEHOLDER}}",
    "name": "{{PLACEHOLDER}}"
  }
}

vars:pre-request {
  shareholderId: {{PLACEHOLDER}}
}

settings {
  encodeUrl: true
  timeout: 0
}

